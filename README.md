# Street Screens - DOOH Advertising Platform

Платформа для управления цифровой наружной рекламой (DOOH - Digital Out-of-Home). Система позволяет управлять сетью цифровых рекламных экранов, создавать и управлять рекламными кампаниями с продвинутым таргетингом, планировать расписание показов и отслеживать аналитику.

## 📋 Описание проекта

Street Screens - это комплексная платформа для управления DOOH-рекламой, которая объединяет управление экранами, кампаниями, таргетингом и аналитикой в единой системе. Платформа предназначена для рекламных агентств, операторов экранных сетей и рекламодателей, которые хотят эффективно управлять цифровой наружной рекламой.

### Что такое DOOH платформа?

DOOH (Digital Out-of-Home) - это цифровая наружная реклама, которая отображается на цифровых экранах в общественных местах: торговых центрах, на улицах, в транспорте, офисных зданиях и других локациях. Платформа позволяет:

- Управлять сетью цифровых экранов
- Создавать таргетированные рекламные кампании
- Планировать расписание показов
- Отслеживать эффективность кампаний
- Управлять бюджетами и счетами

## 🛠 Технологический стек

### Backend

- **Python 3.11+** - основной язык программирования
- **Django 5.2.9** - веб-фреймворк
- **Django REST Framework 3.16.1** - REST API
- **PostgreSQL** - база данных
- **Django REST Auth** - аутентификация и регистрация
- **Simple JWT** - JWT токены для аутентификации
- **Django Allauth** - социальная аутентификация
- **Django CORS Headers** - обработка CORS запросов
- **Django Filter** - фильтрация данных
- **WhiteNoise** - обслуживание статических файлов
- **python-dotenv** - управление переменными окружения

### Frontend

- **React 18.2.0** - библиотека для создания пользовательского интерфейса
- **TypeScript 4.9.5** - типизированный JavaScript
- **Material-UI (MUI) 5.13.3** - компоненты интерфейса
- **Redux Toolkit 1.9.5** - управление состоянием
- **React Router 6.11.2** - маршрутизация
- **React Hook Form 7.44.3** - управление формами
- **Axios 1.4.0** - HTTP клиент
- **Mapbox GL 2.15.0** - интерактивные карты
- **ApexCharts 3.40.0** - графики и диаграммы
- **FullCalendar 6.1.8** - календарь и планирование
- **React PDF 3.1.11** - генерация PDF документов
- **i18next 22.5.0** - интернационализация
- **Framer Motion 10.12.16** - анимации

### Инструменты разработки

- **ESLint** - линтер для JavaScript/TypeScript
- **Prettier** - форматирование кода
- **React Scripts** - инструменты сборки React приложения

## ✨ Основные функции

### 1. Управление экранами (Screen Manager)

- **Управление статусами**: активный, неактивный, на обслуживании
- **Географическое позиционирование**: координаты, регионы, районы
- **Типы локаций**: торговые центры, улицы и дороги, транспортные узлы, офисные здания, спортивные объекты, развлекательные центры
- **Настройка CPM** (Cost Per Mille) для каждого экрана
- **Технические характеристики**: размер экрана, разрешение, тип категории
- **Таргетинг по интересам**: привязка экранов к категориям интересов аудитории
- **Карта экранов**: визуализация всех экранов на интерактивной карте

### 2. Управление кампаниями (Ads Manager)

- **Создание и редактирование кампаний**: полный жизненный цикл рекламных кампаний
- **Управление статусами**: черновик, активная, приостановлена, завершена
- **Бюджетное планирование**: установка бюджета и валюты
- **Планирование расписания**: 
  - Детальное расписание по дням и часам
  - Визуализация расписания в календаре
  - Расчет покрытия расписания
- **Медиа-контент**: 
  - Загрузка видео (AdsManagerVideo)
  - Загрузка изображений (AdsManagerImage)
  - Метаданные медиа-файлов (размер, длительность, разрешение)

### 3. Географический таргетинг

- **Регионы (Regions)**: управление географическими регионами
- **Районы (Districts)**: детализация до уровня районов
- **Гибкий выбор**: таргетинг на регион, район или их комбинацию

### 4. Таргетинг по интересам

- **Категории интересов (Interests)**: нормализованная таксономия интересов (Финансы, Музыка, Образование и т.д.)
- **Множественный выбор**: возможность выбрать несколько категорий интересов
- **Автоматический slug**: генерация URL-friendly идентификаторов

### 5. Типы локаций (Venue Types)

- **Категоризация локаций**: торговые центры, улицы, транспорт, офисы, спорт, развлечения
- **Активность типов**: возможность активировать/деактивировать типы локаций
- **Описания**: детальные описания каждого типа

### 6. Демографический таргетинг

- **Возрастные диапазоны**: таргетинг по возрасту аудитории (min_age, max_age)
- **Гибкая настройка**: возможность комбинировать различные параметры таргетинга

### 7. Управление счетами (Invoices)

- **Генерация счетов**: автоматическое создание счетов для кампаний
- **PDF экспорт**: генерация счетов в формате PDF
- **Фильтрация и поиск**: удобный поиск и фильтрация счетов
- **Аналитика**: статистика по счетам и платежам

### 8. Аналитика

- **Видео аналитика (VideoAnalytics)**: 
  - Отслеживание просмотров видео
  - Длительность просмотра
  - Полнота просмотра
  - Географические данные (страна, город)
  - IP-адреса и User-Agent
- **Статистика кампаний**: агрегированная статистика по кампаниям
- **Статистика экранов**: статистика по статусам и использованию экранов

### 9. Управление пользователями

- **Типы пользователей**: 
  - Ads Client (Рекламный клиент)
  - Ads Manager (Рекламный менеджер)
- **Аутентификация**: 
  - Регистрация по email
  - Вход через JWT токены
  - Обновление токенов
- **Управление профилем**: 
  - Редактирование личных данных
  - Смена пароля
  - Настройки уведомлений
  - Социальные ссылки

### 10. Интерфейс пользователя

- **Адаптивный дизайн**: поддержка мобильных устройств и планшетов
- **Темная/светлая тема**: переключение между темами
- **Многоязычность**: поддержка нескольких языков (i18n)
- **Навигация**: 
  - Вертикальное и горизонтальное меню
  - Breadcrumbs для навигации
  - Поиск по приложению
- **Уведомления**: система уведомлений в реальном времени
- **Настройки**: персонализация интерфейса

## 📁 Структура проекта

```
street-screens/
├── backend/                    # Django backend приложение
│   ├── apps/
│   │   ├── main/              # Основное приложение
│   │   │   ├── models.py     # Модели: Region, District, Interest, VenueType, ScreenManager, AdsManager
│   │   │   ├── admin.py      # Админ-панель Django
│   │   │   ├── urls.py       # URL маршруты
│   │   │   └── migrations/   # Миграции базы данных
│   │   └── users/             # Приложение пользователей
│   │       ├── models.py     # Модель User
│   │       ├── serializers/   # Сериализаторы для API
│   │       │   ├── user.py
│   │       │   └── register.py
│   │       ├── urls.py       # URL маршруты для аутентификации
│   │       └── views.py      # Представления
│   ├── config/                # Конфигурация Django
│   │   ├── settings.py       # Настройки проекта
│   │   ├── urls.py          # Главные URL маршруты
│   │   ├── wsgi.py          # WSGI конфигурация
│   │   └── asgi.py          # ASGI конфигурация
│   ├── manage.py             # Django management скрипт
│   └── requirements.txt      # Python зависимости
│
├── frontend/                  # React frontend приложение
│   ├── public/               # Публичные файлы
│   │   ├── index.html
│   │   ├── assets/           # Статические ресурсы (иконки, изображения)
│   │   └── fonts/            # Шрифты
│   ├── src/
│   │   ├── components/       # Переиспользуемые компоненты
│   │   │   ├── campaign/     # Компоненты кампаний
│   │   │   ├── cards/        # Карточки (campaign-card, screen-card)
│   │   │   ├── map/          # Компоненты карты
│   │   │   ├── schedule/     # Компоненты расписания
│   │   │   └── ...
│   │   ├── sections/         # Основные секции приложения
│   │   │   ├── account/      # Управление аккаунтом
│   │   │   ├── ads-manager/  # Управление рекламой
│   │   │   ├── campaigns/    # Кампании
│   │   │   └── invoice/      # Счета
│   │   ├── layouts/          # Макеты страниц
│   │   │   ├── main/         # Основной макет
│   │   │   ├── auth/         # Макет аутентификации
│   │   │   └── dashboard/    # Макет дашборда
│   │   ├── hooks/            # React хуки
│   │   ├── utils/            # Утилиты
│   │   ├── types/            # TypeScript типы
│   │   ├── theme/            # Тема Material-UI
│   │   ├── locales/         # Переводы (i18n)
│   │   ├── auth/             # Аутентификация
│   │   ├── routes/           # Маршруты приложения
│   │   ├── App.tsx           # Главный компонент
│   │   └── index.tsx         # Точка входа
│   ├── package.json          # Node.js зависимости
│   └── tsconfig.json         # TypeScript конфигурация
│
└── README.md                  # Документация проекта
```

### Ключевые директории и файлы

**Backend:**
- `backend/apps/main/models.py` - основные модели данных (ScreenManager, AdsManager, Region, District и др.)
- `backend/apps/users/models.py` - модель пользователя
- `backend/config/settings.py` - настройки Django (база данных, аутентификация, CORS)
- `backend/requirements.txt` - зависимости Python

**Frontend:**
- `frontend/src/sections/` - основные функциональные секции приложения
- `frontend/src/components/` - переиспользуемые UI компоненты
- `frontend/src/utils/axios.ts` - конфигурация API клиента и эндпоинты
- `frontend/src/types/` - TypeScript типы для всех сущностей
- `frontend/package.json` - зависимости Node.js

## 🚀 Установка и настройка

### Требования

- **Python 3.11+**
- **Node.js 16+** и **Yarn** или **npm**
- **PostgreSQL 12+**
- **Git**

### Установка зависимостей

#### Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

#### Frontend

```bash
cd frontend
yarn install
# или
npm install
```

### Настройка базы данных

1. Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE street_screens;
CREATE USER street_screens_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE street_screens TO street_screens_user;
```

2. Примените миграции:

```bash
cd backend
python manage.py migrate
```

3. (Опционально) Создайте суперпользователя:

```bash
python manage.py createsuperuser
```

### Настройка переменных окружения

Создайте файл `.env` в корне проекта `backend/`:

```env
# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost 127.0.0.1

# Database
POSTGRES_DB=street_screens
POSTGRES_USER=street_screens_user
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000
CORS_ORIGIN_WHITELIST=http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000

# JWT
SIGNING_KEY=your-jwt-signing-key-here
```

### Запуск проекта

#### Backend

```bash
cd backend
source venv/bin/activate  # На Windows: venv\Scripts\activate
python manage.py runserver
```

Backend будет доступен по адресу: `http://localhost:8000`

#### Frontend

```bash
cd frontend
yarn start
# или
npm start
```

Frontend будет доступен по адресу: `http://localhost:3000`

## ⚙️ Конфигурация

### Переменные окружения (.env)

Все настройки проекта управляются через переменные окружения в файле `.env`:

- `SECRET_KEY` - секретный ключ Django (обязательно изменить в продакшене)
- `DEBUG` - режим отладки (True/False)
- `ALLOWED_HOSTS` - разрешенные хосты (через пробел)
- `POSTGRES_*` - настройки подключения к базе данных
- `CORS_ALLOWED_ORIGINS` - разрешенные источники для CORS
- `SIGNING_KEY` - ключ для подписи JWT токенов

### Настройки Django

Основные настройки находятся в `backend/config/settings.py`:

- **Аутентификация**: JWT токены через `rest_framework_simplejwt`
- **Права доступа**: по умолчанию требуется аутентификация
- **Пагинация**: LimitOffsetPagination с размером страницы 50
- **CORS**: настроен для работы с frontend
- **Медиа файлы**: хранятся в `backend/media/`
- **Статические файлы**: обслуживаются через WhiteNoise

### Настройки React

- **API Base URL**: настраивается в `frontend/src/utils/axios.ts`
- **Тема**: настраивается в `frontend/src/theme/`
- **Маршруты**: определяются в `frontend/src/routes/`
- **Языки**: поддерживаются в `frontend/src/locales/langs/`

## 📖 Использование

### Регистрация и вход

1. Откройте приложение в браузере: `http://localhost:3000`
2. Нажмите "Регистрация" или "Вход"
3. Введите email и пароль
4. После успешной аутентификации вы получите JWT токены

### Создание кампании

1. Перейдите в раздел **Ads Manager**
2. Нажмите кнопку **"Создать кампанию"**
3. Заполните форму:
   - Название кампании
   - Бюджет и валюта
   - Даты начала и окончания
   - Выберите регион/район для таргетинга
   - Выберите типы локаций
   - Выберите интересы аудитории
   - Установите возрастной диапазон (опционально)
   - Настройте расписание показов
   - Загрузите медиа-контент (видео/изображения)
4. Сохраните кампанию

### Управление экранами

1. Перейдите в раздел **Screen Manager**
2. Просмотрите список всех экранов
3. Создайте новый экран или отредактируйте существующий:
   - Укажите название и позицию
   - Установите координаты (lat, lng)
   - Выберите регион и район
   - Выберите типы локаций
   - Установите CPM
   - Выберите интересы
   - Установите статус (активный/неактивный/обслуживание)
   - Укажите технические характеристики

### Просмотр аналитики

1. Перейдите в детали кампании
2. Просмотрите статистику:
   - Количество показов
   - Охват аудитории
   - Географическое распределение
   - Аналитика видео (просмотры, длительность, полнота)

### Карта экранов

1. Перейдите в раздел **Map View**
2. Просмотрите все экраны на интерактивной карте
3. Кликните на экран для просмотра деталей

## 🔌 API документация

### Базовый URL

```
http://localhost:8000/api/v1/
```

### Аутентификация

Все запросы (кроме регистрации и входа) требуют JWT токен в заголовке:

```
Authorization: Bearer <access_token>
```

### Основные эндпоинты

#### Аутентификация (`/api/v1/users/`)

- `POST /register/` - Регистрация нового пользователя
- `POST /login/` - Вход в систему
- `POST /logout/` - Выход из системы
- `GET /user/` - Получение информации о текущем пользователе
- `POST /token/verify/` - Проверка токена
- `POST /token/refresh/` - Обновление токена

#### Основные данные (`/api/v1/main/`)

- `GET /screen-managers/` - Список экранов
- `GET /screen-managers/{id}/` - Детали экрана
- `GET /ads-managers/` - Список кампаний
- `GET /ads-managers/{id}/` - Детали кампании
- `GET /regions/` - Список регионов
- `GET /regions/{id}/districts/` - Районы региона
- `GET /districts/` - Список районов
- `GET /interests/` - Список интересов
- `GET /venue-types/` - Список типов локаций

### Примеры запросов

#### Регистрация

```bash
curl -X POST http://localhost:8000/api/v1/users/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password1": "securepassword123",
    "password2": "securepassword123"
  }'
```

#### Вход

```bash
curl -X POST http://localhost:8000/api/v1/users/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "securepassword123"
  }'
```

#### Получение списка кампаний

```bash
curl -X GET http://localhost:8000/api/v1/main/ads-managers/ \
  -H "Authorization: Bearer <access_token>"
```

## 💻 Разработка

### Структура кода

Проект следует принципам чистой архитектуры:

- **Backend**: Django приложения разделены по функциональности (main, users)
- **Frontend**: Компоненты организованы по функциональности и переиспользуемости
- **Типизация**: TypeScript используется для типобезопасности на frontend
- **API**: RESTful API с использованием Django REST Framework

### Стиль кода

**Backend (Python):**
- PEP 8 - стандарт стиля кода Python
- Type hints для всех функций
- Docstrings для классов и функций
- Использование Django best practices

**Frontend (TypeScript/React):**
- ESLint + Prettier для форматирования
- Функциональные компоненты с хуками
- TypeScript для типобезопасности
- Material-UI для компонентов интерфейса

### Запуск линтеров

**Backend:**
```bash
cd backend
flake8 .  # Если установлен
black .   # Если установлен
```

**Frontend:**
```bash
cd frontend
yarn lint
yarn lint:fix
yarn prettier
```

### Тестирование

Для запуска тестов:

**Backend:**
```bash
cd backend
python manage.py test
```

**Frontend:**
```bash
cd frontend
yarn test
```
**Street Screens** - Современная платформа для управления DOOH рекламой
